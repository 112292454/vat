# Future Work

长期规划，非近期计划。

---

## 直播流实时翻译

**思路**：当前 ASR 的处理单位约 30 秒，后续大模型处理虽按 sentence 数量配置但也可以 30 秒为单位。将输入改为直播流（YouTube/B 站直播接口），按固定窗口走完整 pipeline 后输出字幕流，预估端到端延迟约 1 分钟。

**现状**：尚未调研同类实现。需评估是否有成熟的开源方案可直接集成，以及当前框架是否适合扩展为流式处理。优先级低。